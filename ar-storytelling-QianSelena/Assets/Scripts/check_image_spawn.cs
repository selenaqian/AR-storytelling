using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class check_image_spawn : MonoBehaviour
{
    /**public ARTrackedImage qrCode;
    public GameObject goldilocksPrefab;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        Quaternion goldilocksRotation;
        if(this.ARTrackedImage.referenceImage == qrCode) {
            goldilocksRotation.eulerAngles = new Vector3(0, 90, 0);
            Instantiate(goldilocksPrefab, new Vector3(0.1, 0.05, -0.5), goldilocksRotation);
        }
    }*/
}
